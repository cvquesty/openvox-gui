# Force public npm registry for this project.
# This prevents corporate Artifactory / internal mirrors from being baked
# into package-lock.json when developers run `npm install` inside the company.
registry=https://registry.npmjs.org/
