fastapi==0.136.1
starlette==1.3.1
uvicorn[standard]==0.47.0
httpx==0.28.1
pydantic==2.13.4
# pydantic-settings==2.14.2 (security patch): see CHANGELOG.md "Security" section for GHSA-4xgf-cpjx-pc3j details
# (NestedSecretsSettingsSource symlink traversal fix). Applied from PR #37 + full documentation.
# Supply chain: use pip install --require-hashes in production; hashes not pinned here for dev flexibility.
pydantic-settings==2.14.2
sqlalchemy==2.0.49
aiosqlite==0.22.1
asyncpg==0.30.0
psycopg2-binary==2.9.10
pyyaml==6.0.3
python-multipart==0.0.31
jinja2==3.1.6
alembic==1.18.4
passlib[bcrypt]==1.7.4
PyJWT[crypto]==2.13.0
# 4.1+ removes bcrypt.__about__ which passlib 1.7.4 still reads (breaks manage_users)
bcrypt==4.0.1
# 50.0.0: PYSEC-2026-3552/3553/3554 (PKCS#7 oracle, path-build DoS, nameConstraints wildcard)
cryptography==50.0.0
certifi==2026.5.20
prometheus-client==0.25.0
python-json-logger==3.2.1
slowapi==0.1.9
itsdangerous==2.2.0
ldap3==2.9.1

# ovox CLI (shipped with the GUI; installed into the same venv)
typer>=0.12.0
python-dotenv==1.2.2

# For proper HOCON parsing in Puppet Server config files (used by ovox infra tune)
pyhocon>=0.3.63

# Fleet Health PDF report generation (matplotlib for chart recreation matching UI, fpdf2 for fixed-layout one-page PDF assembly)
matplotlib>=3.8
fpdf2>=2.7
Pillow==12.3.0  # fleet PDF/image assembly; pin for OSV-clean (not >=10.0)
