fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
httpx==0.28.1
pydantic==2.13.5
# pydantic-settings==2.15.0 (Dependabot #67). 2.14.2 was GHSA-4xgf-cpjx-pc3j.
# Supply chain: use pip install --require-hashes in production; hashes not pinned here for dev flexibility.
pydantic-settings==2.15.0
sqlalchemy==2.0.52
aiosqlite==0.22.1
asyncpg==0.31.0
psycopg2-binary==2.9.10
pyyaml==6.0.3
python-multipart==0.0.32
jinja2==3.1.6
alembic==1.18.4
PyJWT[crypto]==2.13.0
# Hashing is via bcrypt directly (passlib 1.7.4 is unmaintained and
# breaks on bcrypt 5: missing __about__ + 72-byte ValueError).
bcrypt==5.0.0
# 50.0.0: PYSEC-2026-3552/3553/3554 (PKCS#7 oracle, path-build DoS, nameConstraints wildcard)
# 50.0.1: latest OpenSSL 4.0.2 wheels; no published advisories
cryptography==50.0.1
certifi==2026.7.22
prometheus-client==0.26.0
# 3.2.1 is GHSA-wmxh-pxcx-9w24 / CVE-2025-27607 (dev-extra RCE range).
# No in-repo JsonFormatter import; 4.2.0 is latest and Snyk-clean.
python-json-logger==4.2.0
slowapi==0.1.10
itsdangerous==2.2.0
ldap3==2.9.1

# ovox CLI (shipped with the GUI; installed into the same venv)
typer>=0.27.2
python-dotenv==1.2.3

# For proper HOCON parsing in Puppet Server config files (used by ovox infra tune)
pyhocon>=0.3.63

# Fleet Health PDF report generation (matplotlib for chart recreation matching UI, fpdf2 for fixed-layout one-page PDF assembly)
# 3.8/3.9 are EOL. 3.11.x needs Python >=3.11 and drops the 3.10 floor.
# 3.10.9 is the latest line that still ships cp310 wheels (cycler/eval + latex/ps hardening).
matplotlib>=3.10,<3.11
fpdf2>=2.7
Pillow==12.3.0  # fleet PDF/image assembly; pin for OSV-clean (not >=10.0)
