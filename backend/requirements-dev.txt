# Development / CI extras. Production installs use requirements.txt only.
# Keep these unpinned to a minor so CI picks compatible wheels; bump if a
# release breaks the suite.
pytest>=8.3,<9
pytest-asyncio>=0.24,<1
pytest-cov>=5.0,<7
ruff>=0.16.4,<1
httpx>=0.28
